{
  "name": "meine-musik",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build && node scripts/collect-dist.mjs",
    "build:android": "tauri android build --apk --debug && node scripts/collect-dist.mjs android",
    "android:init": "tauri android init"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-http": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
